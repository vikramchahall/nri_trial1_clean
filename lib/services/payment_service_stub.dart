// This is a blank template. It does nothing but keeps the compiler happy.
void openRazorpayWeb({
  required String key,
  required int amount,
  required String name,
  required String description,
  required Function onSuccess,
}) {
  throw UnsupportedError('Cannot open Razorpay without the correct platform');
}